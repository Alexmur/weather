## Start Backend Server

1. `cd flask`
2. Start the virtual environment: `source bin/activate`
3. Start the server: `python3 server.py`
4. If the server fails to start, run pip install on the missing modules. ex: `pip install flask`


## Start Frontend

1. From the top level directory: `npm start`